@model Restaurant

<section>
    <h3>@Model.Name</h3>
    <div>
        Cuisine: @Model.CuisineType
    </div>

    <div>
        <a asp-controller="Home" asp-action="Details" asp-route-id="@Model.Id">Open</a>
        <a asp-page="/Restaurant/Edit" asp-route-id="@Model.Id">Edit</a>
    </div>
</section>